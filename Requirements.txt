fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
python-dotenv>=1.0

# LangGraph + LangChain
langgraph>=0.2.50
langchain>=0.3.7
langchain-core>=0.3.15

# Gemini provider (default). The OpenAI / Anthropic lines are optional —
# uncomment only if you switch LLM_MODEL to one of those providers.
langchain-google-genai>=2.0
# langchain-openai>=0.2.5
# langchain-anthropic>=0.2.4

# Web-search tool for the research agent. DuckDuckGo via ddgs — no API key.
ddgs>=9.0